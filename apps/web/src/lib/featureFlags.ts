/** Temporary kill-switch for phone-call CTAs and call task creation. */
export const CALLS_ENABLED = false;

/** Contract e-sign UI is not ready; keep APIs but hide the flow. */
export const CONTRACT_SIGNING_ENABLED = false;
