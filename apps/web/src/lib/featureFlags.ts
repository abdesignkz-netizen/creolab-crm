/** Temporary kill-switch for phone-call CTAs and call task creation. */
export const CALLS_ENABLED = false;

/** Contract CMS is verified by official Kalkan sidecar. Tenant still opts in via legal settings. */
export const CONTRACT_SIGNING_ENABLED = true;
